# Design Audit — MusicLounge (Nền tảng bán vé chương trình ca nhạc sống tại phòng trà)

> Kiểm kê toàn bộ page/màn hình theo BƯỚC A của quy trình thiết kế đã thống nhất. Domain: bán vé
> show đã lên lịch (không phải đặt bàn nhà hàng). Trạng thái ghi nhận **trung thực** dựa trên những
> gì đã thực sự làm trong đợt redesign trước đó trong phiên này — không đánh đồng "đã đổi màu/icon
> header" với "đã thiết kế xong theo đúng B0-B3" (visual hierarchy, empty/error/loading state,
> micro-interaction, responsive 3 mốc).

**Chú giải trạng thái**:
- ✅ **Đã redesign sâu** — đã chỉnh cả bố cục/cấu trúc cụ thể cho trang này (không chỉ đổi token màu chung)
- 🟡 **Đã đồng bộ token, chưa polish sâu** — đã có icon-badge header + màu/spacing đúng chuẩn hệ thống, nhưng CHƯA rà theo đủ checklist B2 (empty/error/loading state riêng, micro-interaction, hierarchy có chủ đích)
- ⚪ **Chưa chạm tới** — ngoài phạm vi các đợt trước (Mobile riêng, hoặc nội dung 3D không thuộc phạm vi token UI)

---

## A. Audience — Khám phá & Mua vé (customer journey chính, ảnh hưởng doanh thu trực tiếp)

| Page | File | Trạng thái | Ưu tiên | Ghi chú |
|---|---|---|---|---|
| Dropdown thông báo (navbar) | `components/layout/NotificationDropdown.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | Cao | Việc mới ngoài lô, trước Lô 5. Trước đây bấm chuông ở `AudienceNavbar.jsx` nhảy thẳng `/notifications` — đổi thành popover xem nhanh (~8 thông báo gần nhất) theo chuẩn Facebook/LinkedIn/GitHub, link "Xem tất cả" mới sang trang đầy đủ. Tái dùng `notificationsApi`/`useQuery` có sẵn (không tạo luồng data riêng), tách `NOTIFICATION_REFERENCE_LINK` dùng chung vào `lib/format.js` (trước đó định nghĩa riêng trong `NotificationsPage.jsx`) để cả dropdown và trang đầy đủ luôn đi cùng 1 logic route. Thêm hook `useClickOutside` mới (chưa từng có sẵn — 2 dropdown cũ trong `AudienceNavbar.jsx`, tài khoản/ngôn ngữ, KHÔNG đóng khi click ra ngoài, chỉ đóng khi chọn item; dropdown mới làm đúng chuẩn, 2 dropdown cũ giữ nguyên không đụng vì ngoài phạm vi). Item trong dropdown viết markup gọn riêng (không tái dùng `NotificationRow` card đầy đủ của trang chính) — cùng ngôn ngữ màu (dot brand-500, nền brand-50) nhưng mật độ khác cho phù hợp popover hẹp. Verify qua Playwright: dot đỏ trên chuông khi có chưa đọc, click item không có route → chỉ đánh dấu đã đọc không điều hướng, "Đánh dấu tất cả đã đọc" trong dropdown xoá đúng dot, đóng đúng khi click ra ngoài, "Xem tất cả" vào đúng `/notifications`, empty-state gọn cho user chưa có thông báo, 0 console error. Bell KHÔNG hiện ở 375px — xác nhận đây là do `HomePageMobile.jsx` dùng navbar hoàn toàn riêng (nhóm Mobile ⚪ chưa đụng tới từ trước), không phải bug của việc này |
| Trang chủ (Desktop) | `features/discovery/HomePageDesktop.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | Cao | Lô 3a. Research Eventbrite/Dice (urgency có kiểm soát — "Đêm nay có gì" đã đúng tinh thần 1 show duy nhất, không sửa hướng). **2 bug thật phát hiện qua chụp ảnh**: (1) `Skeleton` mặc định `bg-ink-900/10` gần như vô hình khi đặt trên Hero đã tối sẵn (`bg-ink-900`) — thêm prop `dark` cho `Skeleton.jsx` (nền `bg-white/10` khi `dark`, cùng nguyên tắc với `Badge variant="overlay"`), áp dụng cho Hero. (2) Test ban đầu dùng sai ngày "hôm nay" (giả định 2026-07-30 nhưng đồng hồ hệ thống thật đã sang 2026-07-31) khiến tưởng nhầm TonightSpotlight bị lỗi — sau khi sửa lại dữ liệu test đúng ngày, xác nhận component hoạt động đúng, không phải bug code. <br><br>**Việc mới 2026-07-31 (3 hạng mục, ngoài lô, trước Lô 5):** <br>**(A) Fix "Đêm nay có gì" chỉ hiện 1 show** — Root cause xác định qua đọc code TRƯỚC khi sửa (`TonightSpotlight`, dòng gọi `showsApi.search`): KHÔNG phải bug filter/API, mà do chính frontend tự hardcode `pageSize: 1` khi gọi search — API luôn chỉ được yêu cầu trả đúng 1 kết quả nên `items[0]` không bao giờ có "show thứ 2" để hiển thị dù DB có nhiều show cùng đêm. Sửa: đổi `pageSize: 1` → `5`, thêm state `index` cục bộ + nút mũi tên trái/phải (overlay trắng mờ trên ảnh, cùng ngôn ngữ `backdrop-blur` đã chuẩn hoá) + dot indicator, giữ nguyên cấu trúc khối cam 2 cột hiện có. Verify qua Playwright với 2 show test thật cùng ngày: next/prev/dot chuyển đúng, quay lại đúng show ban đầu, show không có `minPrice` hiện đúng "Liên hệ" (không vỡ layout). <br>**(B) Nâng cấp carousel cuộn ngang** — Rà code xác nhận CHỈ có 1 component carousel thật trong toàn app: `ShowRow` (dùng chung bởi cả "Đề xuất" và mọi "Thể loại X") — 3 nơi khác có `overflow-x-auto` (DashboardPage, ShowDetailManagementPage, StaffLivestreamPage) đều là bảng dữ liệu/code block, không phải carousel, không thuộc phạm vi. Áp dụng `scroll-snap-type`/`scroll-snap-align` + ẩn scrollbar (class `.scrollbar-hide` mới trong `index.css`) + nút mũi tên dùng `scrollIntoView()` (không tự tính pixel) qua hook mới `useHorizontalScroll`. **Bug thật tự phát hiện khi verify** (không phải đoán): nút mũi tên không bao giờ hiện dù rõ ràng còn nội dung tràn — do `useEffect` đo `scrollWidth` chỉ chạy đúng 1 lần lúc mount, mà lúc đó `ShowRow` còn đang ở nhánh Skeleton (chưa gắn ref vào phần tử cuộn thật) nên state bị kẹt ở `false` vĩnh viễn. Sửa bằng callback-ref pattern (đo lại đúng lúc DOM thật gắn vào) + thêm `ResizeObserver` cho chắc. Verify lại xác nhận đúng: nút hiện khi tràn, ẩn hẳn khi cuộn hết 1 phía, 0 console error. <br>**(C) Magnetic hover** — hook mới `useMagneticHover`, áp dụng đúng phạm vi đã chốt (2 link navbar "Khám phá"/"Phòng trà" trong `components/layout/AudienceNavbar.jsx` + 2 CTA chính trong `Hero`), KHÔNG áp dụng ShowCard/LoungeCard. Tắt hoàn toàn qua `matchMedia('(hover: hover) and (pointer: fine)')` (kiểm tra lại mỗi lần `mousemove`) và `prefers-reduced-motion: reduce`; dùng `transition-transform` mặc định của hệ thống (không có nơi nào trong app từng override `duration-*`, giữ đúng timing chung). Verify qua Playwright: transform lệch nhẹ khi di chuột gần + về `matrix(1,0,0,1,0,0)` khi rời chuột; giả lập touch device → `hover:hover` false (hiệu ứng tự tắt); giả lập `prefers-reduced-motion: reduce` → transform giữ nguyên identity dù di chuột gần. Responsive 768px: nav "Khám phá"/"Phòng trà" vốn đã ẩn dưới `lg:` từ trước (hành vi có sẵn, không phải regression). <br><br>**Bổ sung 2026-07-31 (tiếp, cùng ngày) — ArrowCarousel dùng chung + Việc D:** Đọc dữ liệu trước khi code theo đúng yêu cầu: kiểm tra `LoungeDetailDto`/`LoungeShowDetailDto` — cả 2 chỉ có field ảnh đơn (`PrimaryImageUrl`/`CoverImageUrl`), KHÔNG có mảng. Đào sâu thêm phát hiện: bảng `lounge_images` + Domain entity `LoungeImage` + EF configuration **đã tồn tại sẵn trong schema** nhưng **0 dòng dữ liệu**, không có command nào ghi vào bảng này (chỉ có `SetLoungeImageCommand` ghi field đơn `PrimaryImageUrl`, không đụng bảng gallery), và chưa được project vào bất kỳ DTO nào — hạ tầng có sẵn nhưng hoàn toàn chưa nối dây, tương tự gap `ReputationScore` đã ghi nhận trước đó. Theo đúng nhánh quyết định đã chốt: KHÔNG xây gallery cho `LoungeDetailPage` trong lô này — ghi vào "Cần rà soát lại" bên dưới. Refactor `TonightSpotlight` (đã làm ở mục A phía trên) từ state `index` + dot indicator tự chế → dùng component dùng chung mới `components/ui/ArrowCarousel.jsx` (mỗi show là 1 slide toàn chiều rộng, `scrollIntoView()` qua `useHorizontalScroll`), đồng thời refactor luôn `ShowRow` (mục B) để cũng dùng `ArrowCarousel` thay vì tự viết `relative`/nút/`useHorizontalScroll` trực tiếp — 1 component logic carousel duy nhất cho toàn app, không còn 2 bản cài đặt song song. Verify lại qua Playwright xác nhận không có regression: next/prev chuyển đúng show, ẩn/hiện nút đúng biên, 0 console error. **Việc D — icon hover trên nav**: thêm `Compass`/`Building2` (lucide-react, tái dùng đúng icon `Building2` đã dùng cho "phòng trà" ở `LoungeListPage`) ẩn phía trên "Khám phá"/"Phòng trà", `absolute` + `opacity-0` nên không đẩy layout chữ bên dưới. Hiện qua CSS `group-hover` thuần (không cần JS tự phát hiện touch — xác minh trực tiếp qua CSS build ra `@media (hover:hover)` bao quanh biến thể `hover:` của Tailwind v4 trong dự án này, tự động an toàn với touch), `motion-reduce:transition-none` tắt hẳn animation khi bật giảm chuyển động, dùng `transition-all` không override duration (đúng token timing đã dùng ở mục C, không tạo giá trị cứng rời rạc — đã tự sửa lại sau khi lỡ thêm `duration-200` lúc code). Hoạt động độc lập, không xung đột với magnetic hover (C) đã có trên cùng `<Link>`. Verify qua Playwright: icon opacity 0→1 khi hover, về 0 khi rời chuột, không giật layout (ảnh chụp xác nhận trực quan). <br><br>**Sửa lại 2026-07-31 (tiếp) — Việc D làm lại hoàn toàn + thu hẹp phạm vi magnetic:** User phản hồi bản icon-fade-in ở trên gây cảm giác "gớm" vì 2 hiệu ứng (magnetic + icon-reveal) chồng lên cùng 1 chỗ (nav link). Xử lý: (1) **Gỡ hẳn magnetic hover khỏi 2 link nav** — chữ "Khám phá"/"Phòng trà" giờ đứng yên tuyệt đối, magnetic hover chỉ còn ở đúng 2 CTA Hero. (2) **Việc D làm lại theo cơ chế "thẻ 2 lớp dịch chuyển" (vietnam-sketch.com)**, thay hoàn toàn bản icon-fade-in cũ: lớp ngoài (`absolute`, nền brand) + lớp trong (`Link`, nền trắng, viền trong suốt, chứa icon+chữ cố định — không còn fade riêng icon) — hover dịch lớp trong `-translate-x-1 -translate-y-1` (4px, đúng bước đầu thang đo 8pt, tự thu nhỏ so với bản gốc dùng cho ô vuông to), viền chuyển sang `border-brand-500`, `duration-300 ease-out` (giá trị MỚI được yêu cầu riêng cho hiệu ứng này, không phải tự bịa — khác timing mặc định 150ms dùng ở nơi khác). **Bug sub-pixel thật tự phát hiện khi soi ảnh zoom 2x DPI** (mắt thường khó thấy): viền brand lớp ngoài hở 1 viền li ti ở góc bo tròn lúc idle dù `border`/`outline`/`box-shadow` đo qua `getComputedStyle` đều xác nhận trong suốt — do 2 lớp cùng bán kính `rounded-lg` chồng khít `inset-0` không khớp tuyệt đối ở cấp sub-pixel khi AA. Sửa bằng `inset-px` (dùng công cụ Tailwind có sẵn, không hack CSS riêng) làm biên an toàn 1px. **Đo bằng `getComputedStyle` thay vì chỉ nhìn ảnh** — phát hiện thêm 1 điều: Tailwind v4 set hiệu ứng dịch chuyển qua CSS property gốc `translate` (KHÔNG phải `transform`) — verify ban đầu đọc nhầm `transform` nên tưởng hiệu ứng không chạy (`none`), đọc lại đúng property `translate` xác nhận đúng `-4px -4px` khi hover. Đã duyệt bởi user sau khi xem ảnh chụp browser thật (idle/hover, cả bản zoom 2x DPI) + số đo cụ thể. <br><br>**Ghi chú quy trình 2026-07-31**: sau đó có 1 lượt "DEEP RESEARCH" (research motion 5 sản phẩm thật + phương án kiến trúc Hero) đã bị **user yêu cầu hoàn tác toàn bộ** ("trả về trạng thái cũ trước đó") — bao gồm gỡ hẳn hiệu ứng 2 lớp/magnetic và bộ lọc trending Option A. File `docs/motion-guidelines.md` và test `TrendingTests.cs` từ lượt đó đã bị xoá. Ghi lại để tránh nhầm là các quyết định đó đã được duyệt — chúng đã bị rút lại theo đúng yêu cầu, không phải lỗi. <br><br>**Hero carousel nhiều show nổi bật — 2026-07-31 (việc mới, sau khi hoàn tác)**: thay thế hoàn toàn cơ chế cũ (1 nút phụ trỏ cứng `items[0]`) bằng carousel thật. (1) Backend không đổi gì — tái dùng đúng `recommendationsApi.getRecommended`/`showsApi.getTrending` đã có (đã trả sẵn mảng N show), chỉ đổi cách Hero tiêu thụ: `featured.items.slice(0, HERO_SLIDE_COUNT=5)` thay vì `items[0]`. (2) Hero refactor dùng lại `ArrowCarousel` đã xây cho TonightSpotlight/ShowRow (không viết carousel lần 3, đúng DRY) — mỗi show trending là 1 slide đầy đủ (`HeroContent`): ảnh nền/gradient riêng, tên show, ngày, giá (`Từ X đ`/`Liên hệ`), nút "Mua vé ngay" riêng dẫn đúng `/shows/{id}`. (3) Nút "Khám phá chương trình" tách khỏi khái niệm "1 show cụ thể" — render lặp lại y hệt (cùng text/vị trí/đích đến `/search`) trong mọi slide thay vì 1 node overlay riêng (đơn giản hoá, tránh phải tách lớp `pointer-events`, đúng mẫu hình đã lặp ở TonightSpotlight — mỗi slide là 1 đơn vị đầy đủ độc lập) — về mặt thị giác đứng yên tuyệt đối vì nội dung/vị trí không đổi giữa các slide. (4) **Bỏ hẳn magnetic hover khỏi Hero** (cả CTA chính lẫn nút "Mua vé ngay" mới) theo đúng chỉ đạo — hook `useMagneticHover.js` xoá lần 2 (không còn nơi nào dùng trong toàn bộ frontend). CTA dùng đúng `transition-colors duration-200 ease-in-out` (color-only, không transform). Verify qua Playwright: case ≥3 show thật (dữ liệu có sẵn trong DB dev, không cần tạo thêm) — mũi tên next hiện, bấm next chuyển đúng slide, href "Mua vé ngay" đổi đúng theo từng slide (`/shows/6008` → `/shows/8010`), CTA chính đo `translate: none` khi rê chuột gần (xác nhận hết magnetic); case 1 show (mock qua `page.route` để test chính xác biên mà không cần dọn/tạo dữ liệu DB) — mũi tên trái/phải đều ẩn đúng; case 0 show (mock rỗng) — khối "Suất diễn nổi bật" biến mất hoàn toàn, "Khám phá chương trình" vẫn hiện bình thường, không lỗi. 0 console error cả 3 case, build/test/lint sạch. <br><br>**Đơn giản hoá lại — bỏ carousel khỏi Hero (2026-07-31, ngay sau đó)**: user chỉ ra carousel nhiều slide trong Hero đi ngược lại chính research trước đó (Ticketbox/Dice không nhét nhiều show vào Hero) — Hero nên giữ tĩnh, chỉ 1 show nổi bật nhất; nhiều show khác đã có chỗ đúng của nó là khối "Đề xuất" (`ShowRow`, dùng `ArrowCarousel`, đủ sức chứa danh sách dài hơn nhiều). Gỡ bỏ hoàn toàn `ArrowCarousel` khỏi `Hero` — quay về `heroShow` đơn (`featured.items[0]`) thay vì mảng `heroShows`, xoá hằng `HERO_SLIDE_COUNT`. Giữ nguyên `HeroContent` (đã thiết kế sẵn nhận `show=null`/`show` đơn từ trước, không cần viết lại) — chỉ bỏ việc bọc nó trong `ArrowCarousel.map()`. Kết quả: Hero tĩnh (tiêu đề/mô tả không đổi) + đúng 1 khối "Suất diễn nổi bật" (ảnh nền, tên, ngày, giá, nút "Mua vé ngay" riêng) khi có show, ẩn gọn khi không có — không mũi tên, không carousel. Verify qua browser thật: 0 nút mũi tên "Show nổi bật" nào còn tồn tại trong DOM, đúng 1 "Mua vé ngay" trỏ đúng show, "Đêm nay có gì" và "Đề xuất" bên dưới không bị ảnh hưởng, 0 console error, build/test/lint sạch. <br><br>**Đảo vai trò chính/phụ trong Hero — 2026-07-31 (ngay sau đó)**: trước đó show nổi bật chỉ là 1 khối phụ nhỏ ở góc, tagline thương hiệu chung luôn là headline chính — user yêu cầu đảo lại, show là trọng tâm thị giác. **Khi CÓ show**: kicker "ÂM NHẠC SỐNG · PHÒNG TRÀ" giữ nguyên (không đổi vai trò); headline H1 đổi thành TÊN SHOW (cùng cỡ chữ `text-4xl sm:text-5xl` như tagline cũ); ngay dưới là dòng ngày + giá rõ ràng (`DD/MM/YYYY · Từ Xđ`/`Liên hệ`); "Mua vé ngay" trở thành CTA chính (solid trắng, to, dẫn `/shows/{id}`); "Khám phá chương trình" lùi thành CTA phụ (viền, dẫn `/search`); tagline gốc ẩn hẳn (chọn ẩn hoàn toàn thay vì thu nhỏ, sau khi thử bố cục thấy giữ cả dòng mô tả + dòng ngày/giá gây rối, đúng tinh thần "ưu tiên không rối mắt" đã cho phép quyết định qua thử nghiệm). Bỏ luôn nhãn "Suất diễn nổi bật"/icon Flame (dư thừa khi tên show đã là headline). **Khi KHÔNG có show**: fallback y hệt bản gốc trước mọi thay đổi Hero trong phiên này — tagline làm headline, mô tả đầy đủ, chỉ 1 CTA "Khám phá chương trình" — đây là trường hợp DUY NHẤT tagline được làm chính. Ảnh bìa show (nếu có) vẫn dùng làm nền + scrim gradient tối (logic có sẵn từ trước, không đổi) — verify riêng bằng show mock có `coverImageUrl` thật để xác nhận chữ vẫn đọc rõ trên nền ảnh đa dạng, không chỉ trên gradient cam mặc định. Verify qua Playwright + ảnh chụp thật cả 3 case: show không ảnh bìa (dữ liệu thật có sẵn) — H1 đúng tên show, "Mua vé ngay" + "Khám phá chương trình" đều hiện đúng cấp bậc; show có ảnh bìa (mock) — ảnh nền hiển thị đúng, chữ vẫn rõ; 0 show (mock rỗng) — H1 về đúng tagline gốc, "Mua vé ngay" biến mất hoàn toàn. 0 console error cả 3 case, build/test/lint sạch. <br><br>**Nâng cấp "ticket module" + cảnh báo gần hết vé — 2026-07-31 (theo 3 pattern research: atmosphere-first hero/high-contrast ticket module/badge ghim thông tin)**: (1) Đổi "Khám phá chương trình" → "Xem tất cả sự kiện" CHỈ ở nhánh có show (tránh nhầm là dẫn tới chính show đang hiện) — nhánh 0-show giữ nguyên tên cũ vì không có gì để nhầm lẫn. (2) Ảnh bìa show giờ dùng làm nền thật (trước đó dù đã có code fallback nhưng chưa verify với ảnh thật đa dạng nội dung) + scrim gradient tối tái dùng nguyên bản. (3) Gom tên show/ngày/giá/CTA vào 1 "ticket module" riêng — khối bo góc `rounded-3xl`, nền kính mờ (`bg-white/10 backdrop-blur-md`), viền `border-white/15` + `shadow-xl` để tách khỏi ảnh nền thay vì chữ trôi tự do — kicker vẫn nằm ngoài module, đúng vai trò nhỏ không đổi; H1 tên show giữ nguyên kích cỡ lớn bên trong module (không bị hiệu ứng kính làm loãng hierarchy). Cảnh báo "gần hết vé": thêm fetch `useTicketTiers(heroShow?.id)` (hook có sẵn, tái dùng nguyên — chỉ tự chạy khi có `heroShow`, tự skip ở case 0-show/loading, không tốn request thừa), tái dùng **export** `LOW_STOCK_THRESHOLD` từ `TicketPurchasePage.jsx` (trước đó không export, giờ export để tránh định nghĩa lại số 5 ở 2 nơi lệch nhau) — chỉ hiện khi TOÀN BỘ tier của show đều có `availableSlots` giới hạn (nếu có dù chỉ 1 tier không giới hạn, coi như chưa đủ khẩn cấp, không bịa cảm giác gấp). **Bug thật tự phát hiện khi verify** (không phải đoán): dùng `text-warning-300` cho dòng cảnh báo — class này KHÔNG tồn tại trong hệ thống token (`index.css` chỉ định nghĩa `warning-100`/`warning-700`, không có `warning-300`) nên Tailwind không generate ra CSS nào, chữ cảnh báo bị rơi về màu mặc định thay vì màu vàng cảnh báo — soi bằng ảnh chụp ban đầu tưởng đã đúng (nhìn qua tưởng là màu xám mờ hợp lý), nhưng đo `getComputedStyle().color` mới lộ ra không khớp bất kỳ token nào. Sửa dùng `text-warning-100` (`#fef3c7`, sáng — đúng nguyên tắc "shade sáng cho chữ trên nền tối" đã áp dụng nhất quán từ Badge `overlay`/Skeleton `dark` prop), verify lại `getComputedStyle` khớp đúng `rgb(254, 243, 199)`. Verify qua Playwright + ảnh chụp thật 4 case: show có ảnh bìa (không low-stock) — ảnh nền hiện đúng, "Xem tất cả sự kiện" đổi tên đúng, không cảnh báo thừa; show không ảnh bìa — fallback gradient vẫn ổn, ticket module vẫn rõ ràng trên nền gradient; show gần hết vé (mock `availableSlots=3`) — cảnh báo "⚠ Chỉ còn 3 vé" hiện đúng màu, đúng vị trí giữa giá và nút; 0 show — fallback tagline y hệt bản gốc, không đổi. 0 console error cả 4 case, build/test/lint sạch. <br><br>**Tách 2 nhánh theo có/không ảnh bìa — 2026-07-31 (ngay sau đó)**: ticket module kính mờ ở bản trước dùng `max-w-xl`/chung 1 layout cho cả 2 trường hợp, bị phản hồi là vô lý khi không có ảnh (hộp kính "giả vờ có ảnh" nổi trên nền gradient trơn) và chiếm gần nửa Hero khi có ảnh thật (nửa còn lại bỏ trống vô nghĩa). Tách hẳn 2 nhánh không dùng chung layout: **Nhánh A (có ảnh thật)** — thu nhỏ module còn `max-w-sm`, lệch trái/dưới, ảnh làm "atmosphere" chính; **Nhánh B (không ảnh — trường hợp phổ biến với dữ liệu thật hiện có)** — bỏ hẳn khung kính mờ/viền/shadow, quay về đúng bố cục liền mạch của bản "đảo vai trò chính/phụ" trước khi có ticket module. Tách `HeroCtaRow`/`HeroLowStockNote` thành sub-component dùng chung giữa 2 nhánh, tránh lặp JSX nút+cảnh báo. Verify qua Playwright (mock `page.route` cho cả 2 case, không cần dọn/tạo dữ liệu DB): nhánh A đo `boundingBox()` module = 384px / Hero 1232px = tỉ lệ 0.31 (trước ~0.5); nhánh B xác nhận `section .backdrop-blur-md` count = 0 (không còn hộp kính nào sót lại). 0 console error cả 2 case, build/test/lint sạch. Ảnh chụp cả 2 case đã gửi user duyệt trước khi coi là xong (đúng quy trình chờ duyệt). <br><br>**Thêm lại nhãn "Suất diễn nổi bật" — 2026-07-31 (ngay sau đó)**: bước "đảo vai trò chính/phụ" trước đó đã bỏ hẳn nhãn ngữ cảnh (Flame + text) vì lúc đó tên show đã là headline nên nhãn bị coi là dư thừa — nhưng sau khi tên show bị hạ xuống trong ticket module/layout liền mạch (không còn là headline riêng biệt to nhất trang), tín hiệu "đây là show ĐƯỢC CHỌN nổi bật, không phải show ngẫu nhiên" bị mất, cần bổ sung lại. Thêm 1 dòng nhỏ **phía trên kicker "ÂM NHẠC SỐNG · PHÒNG TRÀ"**: `<Flame className="size-3.5" />` + "SUẤT DIỄN NỔI BẬT" (uppercase, `text-[11px]` — tái dùng đúng token size chữ của bản Flame gốc ở bước "Nhãn ngữ cảnh cho CTA phụ" phía trên, chỉ đổi icon `size-3`→`size-3.5` theo yêu cầu mới ~14px). **Đổi màu so với bản gốc**: bản Flame đầu tiên dùng `text-white/60` (trung tính); lần này đổi sang `text-warning-100` theo yêu cầu rõ ràng "màu warning/accent khác màu cam chính" — tái dùng đúng token `warning-100` đã dùng cho cảnh báo gần-hết-vé trong cùng Hero (không bịa token mới), tách biệt rõ với `text-brand-300` của kicker ngay bên dưới (đo `getComputedStyle` xác nhận `rgb(254,243,199)` vs `rgb(226,138,99)` — 2 màu rõ ràng khác nhau). Chỉ hiện khi `show` tồn tại (áp dụng nhất quán CẢ nhánh A lẫn B — nhánh A trước đó cũng thiếu nhãn này, đã kiểm tra và bổ sung theo đúng yêu cầu "nhất quán"), KHÔNG hiện ở fallback 0-show (nhãn gắn với 1 show cụ thể, fallback không đại diện show nào). **Đo chiều cao thật trước/sau** (không suy đoán): Hero dùng `min-height:400px` + `flex-col justify-end` — nội dung cả 3 case (A/B/fallback) đều chưa chạm ngưỡng 400px trước khi thêm nhãn, nên dòng nhãn mới được hấp thụ hoàn toàn bởi khoảng đệm còn dư, đo `boundingBox().height` = đúng 400px cho CẢ 3 case, chênh lệch 0px — không có layout push. Verify qua Playwright + ảnh chụp thật 3 case (mock qua `page.route`): nhánh B (không ảnh) — nhãn hiện rõ trên kicker, layout liền mạch không đổi; nhánh A (có ảnh) — nhãn hiện phía trên module kính (không lọt vào trong module, giữ đúng "không tách thành hộp riêng"); fallback 0-show — 0 nhãn Flame nào xuất hiện (đúng, xác nhận qua `count()`). 0 console error cả 3 case, build/test/lint sạch. <br><br>**Tổng kết hành trình Hero "show nổi bật" (2026-07-31, để đọc lại sau này hiểu lý do quyết định, tránh tưởng nhầm 1 bước nào đó là bug)**: (1) Bắt đầu bằng carousel nhiều slide (`ArrowCarousel` bọc nhiều show) → (2) **bỏ carousel** vì tự mâu thuẫn với chính research đã làm (Ticketbox/Dice giữ Hero tĩnh 1 show, nhiều show thuộc về khối "Đề xuất" riêng có `ArrowCarousel` sẵn) → (3) **đảo vai trò chính/phụ**: tên show trở thành headline to nhất thay vì tagline chung, "Mua vé ngay" thành CTA chính → (4) **thêm ticket module kính mờ** theo research atmosphere-first/high-contrast ticket module + cảnh báo gần-hết-vé thật → (5) **tách 2 nhánh theo có/không ảnh** vì module dùng chung 1 layout cho cả 2 trường hợp vừa vô lý (giả vờ có ảnh khi không có) vừa choán quá nửa Hero khi có ảnh thật → (6) **thêm lại nhãn "Suất diễn nổi bật"** vì bước (3)+(4) đã vô tình làm mất tín hiệu phân biệt "đây là show được chọn nổi bật" khi tên show không còn là headline độc lập to nhất. Mỗi bước đều là quyết định có chủ đích của user qua nhiều vòng xem ảnh chụp thật + đo `getComputedStyle`/`boundingBox`, không phải revert do lỗi — trạng thái CUỐI CÙNG (tính tới thời điểm này) là bước (6) chồng lên (5): Hero tĩnh 1 show, 2 nhánh layout theo ảnh bìa, nhãn "nổi bật" ở cả 2 nhánh, cảnh báo gần-hết-vé khi có dữ liệu thật. <br><br>**Nhãn "nổi bật" thành badge thật — 2026-07-31 (ngay sau đó)**: bản trước chỉ là text uppercase trôi nổi dính sát kicker (`mb-1` = 4px), không đủ tách biệt thị giác. Đổi sang **tái dùng đúng `Badge` component có sẵn** (`components/ui/Badge.jsx`, `variant="warning"` = nền `warning-100` vàng nhạt/chữ `warning-700` amber đậm — không bịa màu mới), icon `Flame` đặt TRONG badge (không còn đứng ngoài). Tăng khoảng cách với kicker từ `mb-1` (4px) lên `mb-3` (12px, gấp 3 lần — vượt yêu cầu tối thiểu gấp đôi). Kicker giữ nguyên style cũ, không đổi. **Bug thật tự phát hiện qua ảnh chụp** (không phải đoán — trước khi có nền màu, lỗi này vô hình): badge render thành 1 thanh dài tràn hết chiều rộng Hero thay vì pill gọn quanh chữ — nguyên nhân là badge (và cả kicker từ trước tới giờ) đều là con trực tiếp của `div.flex.flex-col` không có `items-start`, nên `align-items: stretch` mặc định kéo mọi flex item giãn hết chiều rộng theo trục chéo; kicker (span thường, không nền) trước giờ vẫn bị stretch y hệt nhưng không ai thấy vì không có background để lộ ra. Sửa bằng `self-start` (+ `w-fit` cho chắc) trên chính Badge — không sửa kicker vì không cần (không có nền, không ảnh hưởng thị giác, để nguyên tránh đụng chạm ngoài phạm vi). Verify qua Playwright + `getComputedStyle`/`boundingBox()`: badge width trước sửa = 1136px (tràn hết, đúng bug), sau sửa = 156px (đúng chiều rộng nội dung "🔥 SUẤT DIỄN NỔI BẬT"); gap badge→kicker đo được đúng 12px cả 2 nhánh A/B; màu nền badge `rgb(254,243,199)` = `warning-100`, chữ `rgb(180,83,9)` = `warning-700`, border-radius full pill. 0 console error, build/test(28/28)/lint sạch. <br><br>**Báo cáo thật — vấn đề "Hero còn đơn điệu, nửa phải trống" CHƯA được giải quyết ở lô này**: đây KHÔNG phải phạm vi được yêu cầu sửa trong lượt này (chỉ yêu cầu sửa nhãn), nên không tự ý động vào — chỉ báo cáo trạng thái thật qua ảnh chụp thay vì nhận "xong". Xác nhận qua ảnh `BADGE_B_no_image.png` (nhánh B, không ảnh bìa — trường hợp phổ biến nhất với dữ liệu thật hiện có): toàn bộ nội dung (badge/kicker/H1/ngày-giá/CTA) dồn ở khoảng 60% bên trái, ~40% bên phải Hero vẫn là nền gradient cam-nâu trơn hoàn toàn không có chi tiết gì — đúng là "đơn điệu, nửa phải trống" như đã nêu. Nhánh A (có ảnh bìa thật, `BADGE_A_with_image.png`) KHÔNG còn vấn đề này vì ảnh phủ full-bleed toàn Hero. Gợi ý trước đó (chưa làm, chỉ là ý tưởng optional đã ghi nhận, không tự ý triển khai vì chưa được duyệt): thêm chi tiết trang trí nhẹ (watermark icon nhạc cụ mờ) ở nửa phải cho nhánh B thay vì để trống hoàn toàn — cần user xác nhận hướng trước khi làm, ghi vào "Cần rà soát lại" bên dưới. |
| Tìm kiếm/Kết quả | `features/discovery/SearchPage.jsx`, `FilterPanel.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | Cao | Lô 3a. Research Ticketmaster/StubHub (chip lọc xoá riêng lẻ) + Dice (nút hành động ngay trong empty-state). Thêm chip bộ lọc đang áp dụng (xoá tức thì, không cần bấm lại "Áp dụng" — nhất quán với hành vi "Xoá tất cả" đã có sẵn); thêm nút "Xoá bộ lọc" trực tiếp trong `EmptyState` khi 0 kết quả. **Bug thật phát hiện qua chụp ảnh**: nút "Bộ lọc" hiện số đếm "4" nhưng chỉ thấy 3 chip (do đếm `dateFrom`/`dateTo` là 2 điều kiện riêng trong khi gộp chung 1 chip hiển thị) — hợp nhất lại còn 1 nguồn đếm duy nhất (`filterChips.length`), cùng nguyên tắc với bug Enter-vs-click đã sửa ở `AudienceNavbar` |
| Chi tiết chương trình (Desktop) | `features/shows/ShowDetailPage.jsx` | ✅ **[2026-07-30, verify qua browser thật]** | **Cao** | Rà đủ B2: empty-state ảnh bìa/avatar nghệ sĩ/ảnh phòng trà (fallback icon/gradient thay vì ô trống), skeleton khớp đúng tỉ lệ layout thật, tách CTA chính (Mua vé/Xem trực tiếp/Đặt món) khỏi hành động phụ (Wishlist/Chia sẻ — icon-button 44px chuẩn touch-target), "Báo cáo vi phạm" hạ cấp thị giác xuống dòng riêng. **Bug thật phát hiện qua chụp ảnh browser (không phải đoán)**: badge trạng thái ("Đã duyệt") dùng `Badge variant="neutral"` — nền `bg-ink-900/5` gần như trong suốt, chữ gần như vô hình khi đặt trên hero ảnh/gradient tối. Đã thêm variant `overlay` mới cho `Badge.jsx` (nền trắng mờ + chữ trắng, dùng cho ngữ cảnh đặt trên ảnh/gradient tối) và áp dụng đúng chỗ này — đã kiểm tra không có usage `neutral` nào khác trong app đang nằm trên nền tối tương tự |
| Mua vé (Desktop) | `features/shows/TicketPurchasePage.jsx` | ✅ **[2026-07-30, verify qua browser thật]** | **Cao** | Thêm trạng thái "gần hết vé" (≤5 vé, cảnh báo màu warning) — trước đây chỉ có nhị phân Hết vé/Còn vé; nút Đặt vé disable khi chưa chọn hạng vé (trước đây luôn bấm được, dựa vào toast lỗi); nút +/- số lượng disable đúng biên (min 1, max availableSlots) kèm thông báo khi đạt trần; empty-state dùng `EmptyState` component thay vì `<p>` chuỗi thô; skeleton khớp bố cục 2 cột thật. Đã verify thật bằng Playwright + dữ liệu test thật (tier quota=3): chọn vé → disable/enable nút Đặt vé đúng, spam nút "+" 5 lần chỉ dừng đúng ở 3, cảnh báo "Chỉ còn 3 vé" hiện đúng, tổng tiền tính đúng 450.000đ, 0 console error |
| Kết quả thanh toán | `features/shows/PaymentResultPage.jsx` | ✅ **[2026-07-31, verify nhẹ qua browser thật]** | **Cao** | Lô 3b — verify nhẹ (không rebuild, đã "viết lại hoàn toàn" từ trước). Case thành công, case back/refresh giữa chừng: render đúng, 0 console error. **Vấn đề thật phát hiện, KHÔNG sửa trong lô này** (xem "Cần rà soát lại" — gap kiến trúc backend, vượt phạm vi verify nhẹ): (1) thông báo thất bại luôn là 1 câu chung chung bất kể lý do thật; (2) trang tin tưởng route tĩnh hoàn toàn, không gọi API xác minh — refresh `/payment/success` vẫn hiện "thành công" dù không có giao dịch thật |
| Danh sách phòng trà | `features/lounges/LoungeListPage.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | Cao | Lô 3b. **Bug thật phát hiện qua chụp ảnh**: card lounge thiếu `primaryImageUrl` hiện ô xám trống hoàn toàn, chưa dùng fallback icon đã chuẩn hoá — thêm icon `Building2` (đã import sẵn cho header) vào chỗ ảnh thiếu, cùng pattern với `ShowCard.jsx`/`ShowDetailPage.jsx`. Verify: baseline (nhiều lounge thiếu ảnh) trước/sau đúng, empty-state 0 kết quả rõ ràng, responsive 375/768/1440 đều reflow đúng 1/2/3 cột, 0 console error |
| Chi tiết phòng trà (Desktop) | `features/lounges/LoungeDetailPage.jsx` | ✅ **[2026-07-30, verify qua browser thật]** | Trung | Research Resy/SevenRooms/Airbnb Experiences/Songkick trước khi code (trang xây độ tin cậy, không phải chốt giao dịch). Backend: thêm `Description`/`AtmosphereName` vào `LoungeDetailDto` (data đã có sẵn trong DB, trước đây chưa từng được project ra — đã kiểm tra chỉ 1 consumer duy nhất trước khi sửa, không breaking). Frontend: badge "không khí" + mô tả dưới tiêu đề (có fallback text khi thiếu); tách "Lịch sử hoạt động" thành "Sắp diễn ra"/"Đã diễn ra" (chia theo `scheduledStart` so với hiện tại) — mỗi nhóm có empty-state RIÊNG (không ẩn cả khối) để phòng trà mới không trông giống lỗi tải dữ liệu; gom địa chỉ/follower/số show sắp tới vào 1 khối "thông tin nhanh" dạng icon+dòng ở sidebar; ẩn nút "Tour ảo 3D" khi `model3DUrl` rỗng (trước đây luôn hiện, dẫn khách vào trang 3D rỗng); empty-state ảnh bìa dùng icon toà nhà. Verify qua Playwright với 2 lounge test thật: 1 có đủ mô tả+không khí+cả 2 nhóm show, 1 hoàn toàn rỗng (không mô tả/không khí/không show/không ảnh) — cả 2 case render đúng, 0 console error, 0 bug mới phát hiện |
| Tour ảo 3D | `features/lounges3d/VirtualTourPage.jsx` | ⚪ | Thấp | Trải nghiệm 3D, không thuộc phạm vi token UI 2D thông thường |

> **Sửa lan (component dùng chung) — 2026-07-31**: `components/shows/ShowCard.jsx` (dùng chung bởi Trang chủ/Tìm kiếm/Yêu thích/Chi tiết phòng trà) thiếu icon fallback khi thiếu ảnh bìa — phát hiện khi đọc lại Lô 1-2 trước khi làm Lô 3a (nguyên tắc "card show/lounge phải dùng đúng cách xử lý ảnh thiếu đã chuẩn hoá"). Đã sửa 1 chỗ, **verify qua browser thật ở cả 4 nơi dùng** (không chỉ 2 trang đang làm trong lô này): Trang chủ ✅, Tìm kiếm ✅, Yêu thích ✅ (regression check), Chi tiết phòng trà ✅ (regression check) — không phát sinh vấn đề mới ở bất kỳ nơi nào.

> **Dọn dữ liệu test khỏi Hero/trending — 2026-07-31**: Phát hiện Hero trang chủ hiện show test kỹ thuật "Mux Key Rotate Verify Show" ở vị trí nổi bật nhất. Điều tra trước khi hành động (không tự ý xoá/sửa): (1) Root cause tại `HomePageDesktop.jsx:235` — `<Hero heroShow={featured.items[0]}>` lấy thẳng phần tử đầu của `recommendationsApi.getRecommended(8)` → fallback `GetTrendingFallbackAsync` (user test chưa bật AiConsent) → `LoungeShowRepository.GetTrendingAsync`: lọc `Status IN (Published, Ongoing)`, sắp theo số `user_behaviour_logs` 7 ngày gần nhất DESC — **hoàn toàn không có tiêu chí kiểm duyệt/độ hoàn thiện nội dung nào cho vị trí Hero**, chỉ tin tưởng mù quáng `items[0]`. (2) Đo trực tiếp: "Mux Key Rotate Verify Show" có 27 `user_behaviour_logs`/7 ngày — CAO NHẤT thật sự trong mọi show `Published`/`Ongoing` (nhì là 14), thắng thuật toán đúng luật, không phải tie-break may rủi. Quét DB: 24 show tổng, 21 show tên dạng Test/Verify/Mux Key, 10/21 đang ở trạng thái `Published`/`Ongoing` (đủ điều kiện lọt Hero/trending). (3) Kết luận: **CẢ HAI** — vừa là ô nhiễm dữ liệu môi trường dev (traffic test dồn vào 1 show), vừa là gap logic thật sẽ tái diễn ở production (Hero không có lớp kiểm duyệt nội dung nào, bất kỳ show nào tích luỹ đủ `behaviour_logs` — kể cả nội dung nháp/QA — đều có thể chiếm vị trí này). Đã báo cáo đầy đủ danh sách 21 show (tên + trạng thái + `RecentLogs` + có ảnh/mô tả hay không) cho user xác nhận trước khi hành động. **Hành động đã thực hiện** (chỉ đổi trạng thái, KHÔNG xoá gì — theo đúng chỉ đạo "đổi trạng thái an toàn hơn, không cần hỏi; xoá thì phải hỏi trước"): chuyển 10 show `Published`/`Ongoing` về `Draft` (8 show vốn đã `Draft` và 2 show `Cancelled`/`Ended` không đụng vì đã ngoài diện trending sẵn). Toàn bộ 21 dòng dữ liệu được giữ nguyên (không mất gì) làm mẫu dev/QA — lưu ý không show nào trong 21 show có ảnh bìa thật (chỉ 1 show có ảnh nhưng mô tả 4 ký tự), nên tiêu chí "giữ mẫu sạch có ảnh" không có ứng viên đáp ứng trọn vẹn, đã báo minh bạch thay vì tự chọn liều. Verify qua browser thật sau khi dọn: Hero + "Đề xuất" + mọi "Thể loại X" đều hiện đúng "Đêm Nhạc Trịnh - Tình Xa" (show thật, có nghệ sĩ/thể loại/giá thật) — đúng dự đoán dựa trên số liệu `behaviour_logs` đã đo trước đó; quét toàn bộ text trang chủ xác nhận không còn tên Test/Verify/Mux Key nào; "Đêm nay có gì" tạm ẩn (graceful, không lỗi) vì 2 show từng dùng để test mục A (6009, 7009) đều lên lịch cho hôm nay và nằm trong đợt dọn này — không phải bug, cần tạo lại dữ liệu test mới cho hôm nay nếu cần verify lại tính năng prev/next sau này. 0 console error. **Verify bổ sung 2026-07-31 (cùng ngày)**: kiểm tra riêng trạng thái "0 show hôm nay" của `TonightSpotlight` — lần đầu tiên verify được trạng thái này bằng dữ liệu thật (trước giờ môi trường dev luôn có sẵn show test nên chưa từng lộ ra). Xác nhận qua browser thật: `if (items.length === 0) return null` ẩn gọn **toàn bộ** `<section>` kể cả tiêu đề — DOM query xác nhận 0 heading "Đêm nay có gì", ảnh chụp xác nhận trang chuyển thẳng từ Hero xuống "Đề xuất" không có khung rỗng/tiêu đề trơ trọi/khoảng trắng lạ nào. Không cần sửa gì.

> **Nhãn ngữ cảnh cho CTA phụ ở Hero — 2026-07-31**: Nút phụ trước đây chỉ hiện tên show + ngày, không có tín hiệu phân biệt với nút chính "Khám phá chương trình" (dẫn tới tìm kiếm tổng). Thêm nhãn tĩnh mô tả VAI TRÒ nút (luôn hiện, không phải Badge trạng thái động — không dùng component `Badge` sẵn có vì khác ngữ nghĩa): dòng nhỏ `<Flame className="size-3" /> Suất diễn nổi bật` (uppercase, `text-[11px]`, màu phụ `text-white/60`) phía trên tên show, giữ đúng token màu hệ thống. **Lệch chiều cao thật đo được lúc verify** (không suy đoán): bản đầu `py-2` khiến nút phụ 2 dòng cao 54.5px trong khi nút chính 1 dòng chỉ 44px (chênh 10.5px, đúng dạng lỗi user cảnh báo trước) — sửa bằng `py-1.5` + `leading-none` cho dòng nhãn + `leading-tight` cho dòng tên, đo lại bằng `boundingBox()` xác nhận chênh lệch còn 0.5px (coi như bằng nhau). Verify qua Playwright + ảnh chụp zoom 2x DPI: 2 nút cân đối thị giác tuyệt đối, top/bottom edge thẳng hàng, không lệch layout, 0 console error.

## B. Audience — Sau khi mua & Tài khoản

| Page | File | Trạng thái | Ưu tiên | Ghi chú |
|---|---|---|---|---|
| Vé của tôi (Desktop) | `features/tickets/MyTicketsPage.jsx` | ✅ | Trung | Tab Vé đã mua/Lời mời nhận vé, icon badge |
| Yêu thích | `features/wishlist/WishlistPage.jsx` | ✅ | Trung | Vừa sửa bug bỏ yêu thích không realtime |
| Thông báo | `features/notifications/NotificationsPage.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | Trung | Lô 4b. Thêm nhóm theo ngày (Hôm nay/Hôm qua/Cũ hơn — đủ dùng ở pageSize=50 hiện tại, chưa cần chia nhỏ hơn) và nút "Đánh dấu tất cả đã đọc" (chỉ hiện khi còn ≥1 chưa đọc). Backend: gap thật — chưa có endpoint bulk mark-read — thêm `MarkAllNotificationsReadCommand` + `POST /notifications/read-all` theo đúng CQRS pattern có sẵn (`MarkNotificationReadCommand` làm mẫu), dùng `ExecuteUpdateAsync` (đã là convention có sẵn trong `CancelAbandonedPaymentsJob`), kèm 1 test tích hợp mới xác nhận chỉ đánh dấu đúng thông báo của chính user, không đụng user khác — 148/148 test backend pass. Verify qua Playwright + dữ liệu test thật (4 thông báo trải 3 mốc thời gian, có đọc/chưa đọc): nhóm hiện đúng nhãn, đánh dấu 1 cái riêng lẻ đúng, đánh dấu tất cả xoá hết dot + ẩn nút đúng, empty-state user mới sạch, responsive 375/768/1440 ổn, 0 console error |
| Sở thích & AI đề xuất | `features/account/PreferencesPage.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | Trung | Lô 4b. Rà toàn bộ form (không chỉ header) — pattern chip chọn nhiều đã được xây đúng chuẩn từ trước (không phải checkbox list), không cần đổi. Verify qua Playwright: baseline chưa chọn gì (pill rỗng tự thân đã đủ rõ, không cần thêm empty-state riêng), toggle AI-consent + chọn nhiều chip → lưu → toast thành công → reload vẫn giữ đúng lựa chọn (xác nhận persist thật qua backend, không phải optimistic UI giả), responsive 375/768 ổn, 0 console error. Case lỗi khi mất phiên giữa chừng đã kiểm tra: rơi vào interceptor 401 toàn app có sẵn (`axios.js`, đã có test riêng, hành vi nhất quán mọi trang) — không phải gap riêng của trang này nên không sửa trong lô này |
| Đặt món F&B | `features/fnb/FnbOrderPage.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | **Cao** (đây là trang duy nhất nhóm Trung có luồng giao dịch — nâng lên xử lý đầy đủ B0-B4) | Lô 4a. Research Toast/Square/Disney Mobile Order (duyệt món → +/- ngay tại dòng, không cần màn giỏ hàng riêng) + Starbucks/Highlands (món tạm hết vẫn hiện mờ thay vì biến mất). Backend: đổi `getMenuItems(id, true)` → `getMenuItems(id, false)` để lấy cả món `isAvailable=false` (đã kiểm tra chỉ 1 nơi khác dùng `false` — `FnbMenuManagementPage.jsx`, đúng tiền lệ, không side-effect). Frontend: món hết hàng hiện mờ (`opacity-50`) + disable nút +/- + nhãn `(hết hàng)` — tái dùng NGUYÊN VĂN chữ và pattern đã có sẵn ở trang quản lý Owner, không tự chế; thêm icon `GlassWater` fallback khi thiếu ảnh món. Verify qua Playwright + dữ liệu test thật (chèn 1 món test `isAvailable=false` vào menu có sẵn): món hết hàng hiện đúng mờ/disable/nhãn, thêm 2x món thường vào giỏ → tổng tiền đúng 50.000đ, đặt món thành công (mã đơn thật), chưa đăng nhập → redirect `/login` đúng (qua `ProtectedRoute`, không cần chạm nút), responsive 375/768/1440 sticky bar không che nội dung (xác nhận bằng screenshot viewport thật sau khi phát hiện `fullPage` screenshot của Playwright có artifact hiển thị sai vị trí `position:fixed` — không phải bug thật, đã dùng screenshot viewport thường + cuộn thật để xác minh lại). 0 console error |
| Gửi khiếu nại | `features/complaints/ComplaintFormPage.jsx` | ✅ | Trung | Step indicator, icon badge |
| Khiếu nại của tôi | `features/complaints/MyComplaintsPage.jsx` | ✅ | Trung | Icon badge, shadow-sm |
| Xem Livestream | `features/livestream/LivestreamWatchPage.jsx` | ✅ | Trung | Nền tối có chủ đích (ngữ cảnh xem trực tiếp, khác Audience nền sáng) |

## C. Auth

| Page | File | Trạng thái | Ưu tiên | Ghi chú |
|---|---|---|---|---|
| Đăng nhập/Đăng ký/Quên-Đặt lại mật khẩu | `features/auth/{Login,Register,ForgotPassword,ResetPassword}Page.jsx` | ✅ | Trung | Có `AuthBackdrop` nền thương hiệu, đã redesign |

## D. Owner — Quản lý phòng trà (nội bộ, không trực tiếp khách hàng)

| Page | File | Trạng thái | Ưu tiên |
|---|---|---|---|
| Bảng điều khiển | `features/manager/DashboardPage.jsx` | ✅ | Thấp |
| Quản lý chương trình | `features/manager/ShowManagementPage.jsx` | ✅ | Thấp |
| Tạo chương trình (wizard) | `features/manager/CreateShowWizard.jsx` | ✅ | Thấp |
| Chi tiết quản lý 1 show | `features/manager/ShowDetailManagementPage.jsx` | ✅ **[2026-07-31, verify qua browser thật]** | Thấp |
| Quản lý phòng trà | `features/manager/LoungeManagementPage.jsx` | ✅ | Thấp |
| Tạo phòng trà | `features/manager/CreateLoungeForm.jsx` | ✅ **[2026-07-31, verify qua browser thật — bắt được 1 bug thật]** | Thấp |
| Gói subscription | `features/manager/SubscriptionPage.jsx` | ✅ | Thấp |
| Donate | `features/manager/OwnerDonationsPage.jsx` | ✅ | Thấp |
| Nhân viên | `features/manager/StaffManagementPage.jsx` | ✅ | Thấp |
| Thực đơn F&B | `features/manager/FnbMenuManagementPage.jsx` | ✅ | Thấp |

> **Lô 5a — 2026-07-31 (`ShowDetailManagementPage.jsx` + `CreateLoungeForm.jsx`)**: verify NHẸ theo đúng phạm vi (trang nội bộ, ưu tiên Thấp) — không rebuild toàn diện, chỉ đào sâu sửa khi phát hiện vấn đề thật. Tham chiếu nhanh (không research sâu): cả 2 file đã sẵn đúng 2 pattern B2B/admin phổ biến — trang chi tiết 1 entity dạng tab (Tổng quan/Đơn hàng/Sơ đồ ghế/Chỉnh sửa, giống Stripe Dashboard/Shopify Admin) và khoá field sửa theo giai đoạn vòng đời (chỉ sửa tên/mô tả khi còn Draft, giống pattern "immutable after publish" phổ biến ở các nền tảng đặt vé) — không cần thay đổi hướng thiết kế.
>
> **Verify `ShowDetailManagementPage`** qua browser thật (owner test account thật, dữ liệu thật trong DB dev — show Draft `7009` và show Published `6008` có 6 vé đã huỷ thật): cả 4 tab (Tổng quan/Đơn hàng/Sơ đồ ghế/Chỉnh sửa) render đúng dữ liệu ở cả 2 trạng thái show; `OverviewTab` tính đúng "Vé đã bán"/"Doanh thu" chỉ đếm vé `Confirmed` (0/0 đúng vì cả 6 vé show 6008 đều `Cancelled` — xác nhận logic lọc đúng, không tính nhầm vé đã huỷ); `OrdersTab` render đúng badge `danger` "Đã huỷ"; `EditTab` khoá đúng (hiện thông báo hướng dẫn dùng "Quản lý vòng đời") khi Published, mở form sửa + lưu thành công (toast + H1 cập nhật tên mới) khi Draft; `SeatmapTab` hiện đúng 4 khu vực với % lấp đầy; modal "Quản lý vòng đời" mở đúng, hiện đúng gate nghiệp vụ "Tác quyền VCPMC (bắt buộc trước khi bắt đầu)" khi show còn Draft. Responsive 768px: không tràn ngang, thẻ số liệu tự xuống 2 cột — chấp nhận được cho trang nội bộ (sidebar cố định không thu gọn ở mọi trang `/manager/*`, đây là đặc điểm toàn cục của `ManagerLayout`, không phải lỗi riêng trang này — không tự ý sửa vì ngoài phạm vi Lô 5a). 0 console error, 0 lỗi mạng.
>
> **Verify `CreateLoungeForm`** qua browser thật (tài khoản Owner mới đăng ký, chưa có phòng trà nào): validate đúng khi submit rỗng (4 field bắt buộc đều báo lỗi tiếng Việt cụ thể + viền đỏ); cascading Tỉnh/Thành→Phường/Xã hoạt động đúng (chọn Hà Nội → danh sách phường cập nhật đúng, ward select tự khoá tới khi chọn tỉnh). **Bắt được 1 bug thật (không phải suy đoán — đọc code xác nhận root cause, tái hiện qua browser, sửa xong verify lại)**: sau khi tạo phòng trà thành công (toast hiện đúng, DB có bản ghi mới), form KHÔNG chuyển sang bước "Hoàn thiện phòng trà" (upload ảnh + khai báo khu vực chỗ ngồi) như code kỳ vọng — quay lại y hệt form rỗng ban đầu. Root cause: `onSubmit` gọi `refetchLounges()` (=`refetch` từ `useMyLounges()`, sống ở `ManagerLayout`) NGAY TRƯỚC `setCreatedLoungeId(id)`; `ManagerLayout.jsx` có `if (loading) return <skeleton-only, KHÔNG co Outlet>` — mỗi lần `loading` bật lại `true` do refetch, toàn bộ `<Outlet>` (chứa `CreateLoungeForm`) bị unmount rồi remount fresh, xoá sạch state `createdLoungeId` vừa set. Đây là bug tầng layout dùng chung, không riêng gì `CreateLoungeForm` — bất kỳ trang con nào gọi `refetchLounges()` trong lúc giữ state cục bộ đều gặp lỗi tương tự (vd `LoungeManagementPage`/`FnbMenuManagementPage` cũng dùng `refetchLounges` — chưa phát hiện triệu chứng ở các trang đó vì chúng không giữ state chuyển-bước cần bảo toàn qua remount, nhưng cùng cơ chế rủi ro). Sửa tại `ManagerLayout.jsx`: đổi điều kiện `if (loading)` → `if (loading && !lounges)` (chỉ hiện skeleton toàn trang ở lần tải ĐẦU TIÊN, các lần refetch sau giữ nguyên `<Outlet>` đang mount). Verify lại: đăng ký tài khoản Owner mới thứ 2, lặp lại đúng luồng — bước "Hoàn thiện phòng trà" hiện đúng ngay sau submit VÀ vẫn giữ nguyên sau khi đợi thêm 2s (loại trừ khả năng remount trễ), "Chưa có khu vực nào" (empty state) hiện đúng, "Chọn ảnh"/"Thêm khu vực mới" đều sẵn sàng thao tác. 0 console error, 0 lỗi mạng, lint/test(28/28)/build sạch sau khi sửa.

## E. Staff — Vận hành tại chỗ

| Page | File | Trạng thái | Ưu tiên |
|---|---|---|---|
| Check-in vé | `features/staff/CheckInPage.jsx` | 🟡 | Thấp |
| Vận hành Livestream | `features/staff/StaffLivestreamPage.jsx` | 🟡 | Thấp |
| Bán vé tại quầy | `features/staff/WalkInSalePage.jsx` | 🟡 | Thấp |
| Order F&B | `features/staff/FnbOrdersPage.jsx` | ✅ | Thấp |

## F. Admin — Kiểm duyệt & Nền tảng

| Page | File | Trạng thái | Ưu tiên |
|---|---|---|---|
| Khiếu nại | `features/admin/AdminComplaintsPage.jsx` | ✅ | Thấp |
| Hoàn tiền | `features/admin/AdminRefundsPage.jsx` | ✅ | Thấp |
| Gói subscription (Admin) | `features/admin/AdminSubscriptionPackagesPage.jsx` | ✅ | Thấp |
| Duyệt nội dung | `features/admin/AdminModerationPage.jsx` | ✅ | Thấp |
| Thống kê nền tảng | `features/admin/AdminPlatformAnalyticsPage.jsx` | ✅ | Thấp |

## G. Mobile — ngoài phạm vi các đợt trước (chưa chạm)

| Page | File | Trạng thái |
|---|---|---|
| Trang chủ Mobile | `features/discovery/HomePageMobile.jsx` | ⚪ |
| Chi tiết show Mobile | `features/shows/ShowDetailMobile.jsx` | ⚪ |
| Chi tiết phòng trà Mobile | `features/lounges/LoungeDetailMobile.jsx` | ⚪ |
| Mua vé Mobile | `features/shows/TicketPurchaseMobile.jsx` | ⚪ |
| Vé/Yêu thích Mobile | `features/tickets/MyShowMobile.jsx` | ⚪ |

---

## Đề xuất thứ tự xử lý (chờ bạn duyệt/điều chỉnh)

Dựa theo đúng logic bạn nêu (ưu tiên theo vai trò trong customer journey + mức độ đã "polish sâu"
hay chưa — ưu tiên page **Cao** mà còn 🟡/chưa rà kỹ trước page đã ✅ rồi):

1. ~~`TicketPurchasePage.jsx`~~ — ✅ xong Lô 1 (2026-07-30).
2. ~~`LoungeDetailPage.jsx`~~ — ✅ xong Lô 2 (2026-07-30).
3. ~~Lô 3a: `HomePageDesktop.jsx` + `SearchPage.jsx`~~ — ✅ xong (2026-07-31).
4. ~~Lô 3b: `PaymentResultPage.jsx` + `LoungeListPage.jsx`~~ — ✅ xong (2026-07-31, verify nhẹ).
5. ~~Lô 4a: `FnbOrderPage.jsx`~~ — ✅ xong (2026-07-31, B0-B4 đầy đủ).
6. ~~Lô 4b: `NotificationsPage.jsx` + `PreferencesPage.jsx`~~ — ✅ xong (2026-07-31). Nhóm Trung (B) đã xong toàn bộ.
7. ~~Lô 5a: `ShowDetailManagementPage.jsx` + `CreateLoungeForm.jsx`~~ — ✅ xong (2026-07-31, verify nhẹ + 1 bug thật ở tầng `ManagerLayout.jsx` được sửa).
7b. **Lô 5b (tiếp theo, chờ duyệt)**: nhóm Staff — `CheckInPage.jsx`, `StaffLivestreamPage.jsx`, `WalkInSalePage.jsx`.
8. Mobile (nhóm G) + Tour 3D — không bỏ qua, xử lý ở cuối theo đúng yêu cầu (không còn trạng thái "ngoài phạm vi vĩnh viễn").

**Cần rà soát lại** (phát hiện lúc code, KHÔNG tự ý xây/sửa lan — chờ quyết định riêng):
- **`MusicLounge.ReputationScore`** (`src/MusicLounge.Domain/Entities/MusicLounge.cs:19`) — field tồn tại trong DB schema với comment nghiệp vụ "D3: star-rating scale 0–5, thresholds at 3.5/4.2" (gợi ý có hệ thống badge uy tín theo bậc) nhưng **chưa từng được tính toán hay set giá trị ở bất kỳ đâu trong codebase**, cũng chưa được project ra bất kỳ DTO nào. Đây là gap tính năng lớn hơn 1 trang UI — cần quyết định kiến trúc trước khi xây: (a) tính on-demand mỗi lần query (đơn giản, nhưng tốn chi phí tính nếu nhiều rating) hay (b) tính qua background job định kỳ ghi vào cột có sẵn (nhất quán với pattern Hangfire job đã dùng cho các tác vụ định kỳ khác trong dự án, nhưng cần quyết định tần suất chạy). Không tự ý chọn phương án — chờ xác nhận.
- **`Skeleton` trên nền tối ở nơi khác** — sau khi thêm prop `dark` cho `Skeleton.jsx` (Lô 3a), phát hiện 14 chỗ khác trong app đang tự override thủ công bằng `className="... bg-white/10"` thay vì dùng prop chuẩn hoá (chủ yếu ở các file Mobile — `HomePageMobile`, `LoungeDetailMobile`, `ShowDetailMobile`, `TicketPurchaseMobile`, `MyShowMobile` — và `VirtualTourPage`/`SeatingMapPicker` thuộc phạm vi 3D). Không tự ý sửa vì ngoài phạm vi Desktop-first hiện tại — nếu sau này làm tới nhóm Mobile/3D, nên đổi các chỗ đó sang dùng `dark` prop để nhất quán, không phải việc bắt buộc ngay.
- **VNPay: mã lỗi thanh toán bị bỏ khi redirect, không tới được UI** (phát hiện Lô 3b, 2026-07-31) — Root cause: `ProcessVnPayCallbackCommandHandler` (`src/MusicLounge.Application/Tickets/Commands/ProcessVnPayCallback/`) có lưu `Payment.VnPayResponseCode` vào DB, nhưng `PaymentsController.VnPayCallback` (`src/MusicLounge.Api/Controllers/PaymentsController.cs`) redirect bằng URL tĩnh từ `BusinessSettings` (`_settings.PaymentSuccessUrl`/`PaymentFailedUrl`) **không kèm query param nào** — mã lỗi thật (hết hạn phiên, sai thẻ, huỷ giữa chừng, lỗi hệ thống VNPay...) không bao giờ tới được frontend. Hệ quả UI: `PaymentResultPage.jsx` chỉ hiện được 1 câu thông báo thất bại chung chung cho MỌI nguyên nhân, và trang cũng không gọi API xác minh gì (tin route tĩnh tuyệt đối — refresh `/payment/success` vẫn hiện "thành công" dù không có giao dịch thật). **Phạm vi ảnh hưởng**: pattern `Redirect(_settings.XxxSuccessUrl) : Redirect(_settings.XxxFailedUrl)` giống hệt bị lặp lại ở `DonationsController.cs` và `SubscriptionsController.cs` — không riêng thanh toán vé. **Đề xuất hướng xử lý** (chưa làm, cần 1 lô backend riêng): (a) thêm query param mã lỗi VNPay (và có thể `orderId`) vào cả 2 nhánh redirect ở cả 3 controller; (b) xây bảng ánh xạ mã lỗi VNPay → thông điệp tiếng Việt cụ thể ở frontend (`PaymentResultPage.jsx` và tương đương cho donation/subscription nếu có trang riêng); (c) cân nhắc việc trang có nên gọi API xác minh trạng thái giao dịch thật thay vì tin tuyệt đối vào route tĩnh. Quyết định của user (2026-07-31): không sửa trong Lô 3b, để lại làm 1 lô riêng sau.
- **Hero nhánh B (không ảnh bìa) còn "đơn điệu, nửa phải trống"** (nêu lại 2026-07-31, chưa xử lý) — với dữ liệu thật hiện có (đa số show chưa có `coverImageUrl`), Hero rơi vào nhánh B: toàn bộ nội dung (badge/kicker/H1/ngày-giá/CTA) dồn ~60% bên trái, ~40% bên phải chỉ là nền gradient cam-nâu trơn không chi tiết — xác nhận qua ảnh chụp thật (`BADGE_B_no_image.png`), không phải suy đoán. Ý tưởng đã nêu nhưng CHƯA triển khai (chỉ optional, cần user chọn hướng trước): thêm chi tiết trang trí nhẹ (watermark icon nhạc cụ mờ, ví dụ `lucide-react` `Music`/`Guitar` với opacity thấp) lấp nửa phải cho riêng nhánh B, thay vì để trống hoàn toàn hoặc giả vờ có ảnh. Không tự ý làm vì đây là quyết định thẩm mỹ cần duyệt trước, không phải bug.
- **Gallery ảnh phòng trà (`lounge_images`) có schema nhưng chưa nối dây** (phát hiện 2026-07-31 khi rà dữ liệu cho ArrowCarousel) — Bảng `lounge_images` + Domain entity `LoungeImage` (`src/MusicLounge.Domain/Entities/LoungeImage.cs`) + `LoungeImageConfiguration` đã tồn tại đầy đủ trong schema (`LoungeId, ImageUrl, Caption, DisplayOrder, IsPrimary, UploadedAt`) nhưng **0 dòng dữ liệu trong DB**, và **không có command/query nào chạm tới bảng này** — `SetLoungeImageCommand` (`src/MusicLounge.Application/Lounges/Commands/SetLoungeImage/`) chỉ ghi field đơn `MusicLounge.PrimaryImageUrl`, không phải bảng gallery. `LoungeDetailDto` cũng chưa có field `Images`/`galleryUrls` nào. Cùng dạng gap với `ReputationScore` (hạ tầng DB có sẵn, tầng Application/UI chưa từng xây). **Đề xuất hướng xử lý** (chưa làm, cần quyết định + 1 lô riêng vì đụng cả backend lẫn Owner UI): (a) thêm `AddLoungeImageCommand`/`RemoveLoungeImageCommand`/`ReorderLoungeImagesCommand` theo đúng pattern CQRS + ownership check hiện có; (b) mở rộng `LoungeDetailDto` thêm `IReadOnlyList<LoungeImageDto> Images`; (c) xây UI quản lý gallery cho Owner (`LoungeManagementPage.jsx`, tương tự `LoungeImageUpload.jsx` hiện có nhưng cho nhiều ảnh); (d) chỉ sau đó mới gắn `ArrowCarousel` vào `LoungeDetailPage` hero để chuyển giữa ảnh không gian thật. Không tự ý xây trong lô này.

*Cập nhật lần đầu: 2026-07-29. Lô 1-2 hoàn tất 2026-07-30, Lô 3a hoàn tất 2026-07-31, Lô 3b hoàn tất 2026-07-31, Lô 4a hoàn tất 2026-07-31, Lô 4b hoàn tất 2026-07-31, NotificationDropdown + 3 hạng mục UX (TonightSpotlight nhiều show/carousel scroll-snap/magnetic hover) + ArrowCarousel dùng chung + icon hover nav (Việc D) hoàn tất 2026-07-31 — ngoài lô, trước Lô 5, Hero "show nổi bật" (nhiều vòng lặp: carousel→bỏ→đảo hierarchy→ticket module→tách 2 nhánh→badge "nổi bật") hoàn tất 2026-07-31, Lô 5a (`ShowDetailManagementPage.jsx` + `CreateLoungeForm.jsx`, verify nhẹ + fix bug `ManagerLayout.jsx`) hoàn tất 2026-07-31 (kèm verify browser thật, không chỉ build/lint/test).*
