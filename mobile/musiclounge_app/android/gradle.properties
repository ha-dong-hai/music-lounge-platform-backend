org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin's incremental compiler cache crashes when the project (J:) and the
# pub-cache/Gradle caches (C:) live on different drive letters on Windows --
# it can't compute a relative path across roots. Incremental compilation is
# a build-speed optimization only; disabling it just makes rebuilds slower.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
