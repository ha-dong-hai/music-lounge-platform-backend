# Backend mà Vite dev server proxy tới.
# Bỏ trống / không có file này  -> dùng backend thật trên Azure (mặc định trong vite.config.ts).
# Muốn chạy với API local       -> copy file này thành `.env.local` và bỏ comment dòng dưới.
# VITE_BACKEND_TARGET=http://localhost:5289
